#include <iostream>
#include <sstream>

using namespace std;

int main() {
    //code
    int T, N;
    cin >> T;
    for (int i = 0; i < T; i++) {
        string s;
        cin >> s;
        cin >> N;
    }
    return 0;
}